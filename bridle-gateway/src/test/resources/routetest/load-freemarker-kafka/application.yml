spring:
  output:
    ansi:
      enabled: always
camel:
  component:
    kafka:
      enabled: false
    http:
      enabled: false
    scheduler:
      enabled: false
    freemarker:
      enabled: false

gateway:
  type: "load-freemarker-kafka"
main-scheduler:
  poolSize: "1"
  endpoint-properties:
    delay: "100"
main-freemarker:
  resourceUri: "/routetest/load-freemarker-kafka/load.tmpl"
kafka-out:
  brokers: "localhost:9090"
  topic: "routetest"
